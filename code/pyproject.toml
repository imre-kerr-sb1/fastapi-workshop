[project]
name = "fastapi-workshop"
version = "0.1.0"
description = "Code scratchpad for fastapi workshop"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.140.13",
]
